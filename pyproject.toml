[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "av-cli"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = ["click>=8.0"]

[project.scripts]
av = "av:cli"

[tool.setuptools]
py-modules = ["av"]
